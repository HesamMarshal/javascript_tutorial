// 

const myMessage = "This is a JS Course!";
console.log(myMessage);

const split1 = myMessage.split(" ");
console.log(split1);

// join
const joined = split1.join("-");
console.log(joined);


// Chian
const chainedJoined = myMessage.split(" ").join("-");
console.log(chainedJoined);